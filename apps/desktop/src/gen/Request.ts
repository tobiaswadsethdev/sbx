// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { NewComment } from "./NewComment";
import type { NewSession } from "./NewSession";

/**
 * Something a client asks the server to do.
 *
 * Tagged by `op` rather than positionally, so a message stays readable in a log
 * and an unknown variant is a name rather than an index.
 */
export type Request = { "op": "ls" } | { "op": "poll", name: string, } | { "op": "diff", name: string, } | { "op": "policy", name: string, } | { "op": "events", name: string, } | { "op": "repos" } | { "op": "inspect", path: string, 
/**
 * The branch to measure against; `None` means the checkout's own.
 */
branch: string | null, } | { "op": "new-options" } | { "op": "comments", name: string, } | { "op": "comment", name: string, comment: NewComment, } | { "op": "uncomment", name: string, id: number, } | { "op": "send-comments", name: string, } | { "op": "create" } & NewSession;

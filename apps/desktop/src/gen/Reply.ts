// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { Event } from "./Event";
import type { Listing } from "./Listing";
import type { NewOptions } from "./NewOptions";
import type { Picked } from "./Picked";
import type { Poll } from "./Poll";
import type { Session } from "./Session";
import type { View } from "./View";

/**
 * What the server sends back when it worked.
 */
export type Reply = { "reply": "ls", sessions: Array<Session>, 
/**
 * Sessions recovered from a sandbox the cache did not know about, and
 * ones whose sandbox has just gone. Carried because reconciliation is
 * the server's to do and the client would otherwise see sessions
 * appear and vanish with no account of why.
 */
adopted: Array<string>, dead: Array<string>, warnings: Array<string>, } | { "reply": "poll" } & Poll | { "reply": "diff", body: string, } | { "reply": "policy" } & View | { "reply": "events", events: Array<Event>, } | { "reply": "repos" } & Listing | { "reply": "inspect" } & Picked | { "reply": "new-options" } & NewOptions | { "reply": "created", name: string, };
